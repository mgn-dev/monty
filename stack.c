#include "monty.h"

/**
 * stack - rotls the top two elements of the stack.
 * @head: pointer to the linked list.
 * @ln: instruction line number.
 *
*/
void stack(stack_t **head, unsigned int ln)
{
	(void)head;
	(void)ln;

	gob.mode = STACK;
}
