#include "monty.h"

/**
 * nop - function that prints top of the stack.
 * @head: pointer to the linked list.
 * @ln: instruction line number.
 *
*/
void nop(stack_t **head, unsigned int ln)
{
	(void)head;
	(void)ln;
}
